# 

## Problem

## Context

* A CDC topic includes event_status which has three relevant columns:
    ```sql
    CREATE TABLE event_status (
        
    )
    ```
eventId
eventProcessed - boolean which switches from false to true when an event has been processed
eventTime