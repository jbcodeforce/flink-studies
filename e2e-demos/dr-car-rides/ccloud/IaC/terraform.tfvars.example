# Copy to terraform.tfvars and fill in values.
# Prefer env vars for secrets: CONFLUENT_CLOUD_API_KEY / CONFLUENT_CLOUD_API_SECRET

owner_email    = "you@example.com"
prefix         = "dr-rides"
primary_region = "us-west-2"
dr_region      = "us-east-1"

# Optional — leave empty to use environment variables
# confluent_cloud_api_key    = ""
# confluent_cloud_api_secret = ""

flink_max_cfu = 10

# Incremental build flags (defaults match iteration 1)
enable_cluster_link   = false # iteration 2: topics + Cluster Linking + mirrors
enable_schema_linking = false # iteration 2: SR IMPORT + exporter
enable_tableflow      = false # later: AWS S3/Glue + Tableflow PI

# After first apply of provider integration, set external ID from Confluent UI
# and re-apply so the IAM trust policy is locked down:
# confluent_external_id = ""
