apiVersion: platform.confluent.io/v1beta1
kind: Schema
metadata:
  name: raw-contract-value
  namespace: confluent
spec:
  schemaRegistryClusterRef:
    name: schemaregistry
    namespace: confluent
  data:
    configRef: raw-contract-value
    format: json 
  compatibilityLevel: BACKWARD