apiVersion: platform.confluent.io/v1beta1
kind: Schema
metadata:
  name: order-simple-value
  namespace: confluent
spec:
  schemaRegistryClusterRef:
    name: schemaregistry
    namespace: confluent
  data:
    configRef: order-simple-value
    format: json 
  compatibilityLevel: BACKWARD