apiVersion: platform.confluent.io/v1beta1
kind: KafkaTopic
metadata:
  name: perf-input
  namespace: kafka
spec:
  replicas: 1
  partitionCount: 1
  configs:
    cleanup.policy: "delete"
  kafkaRestClassRef:
    name: default
    namespace: kafka