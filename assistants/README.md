# Assistants

A set of tools to help maintaining this repository and do semantic search.

## Knowledge indexation

- **[km-agno](km-agno/README.md)** — Agno + Chroma: index repo files for semantic `search` and RAG `ask` (see `uv` setup in that folder).
